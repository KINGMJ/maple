{{#foreach tags}}
    <a href="{{url}}" style="background-image: url('{{image}}')">
        <div class="overlay">
            <h2>{{name}}</h2>
            <button class="info">Show</button>
        </div>
    </a>
{{/foreach}}

